export enum DefaultSystemRoles {
  ADMIN = "Admin",
  APPROVER = "Approver",
  REQUESTOR = "Requestor",
}

// If any new roles gets added, update the same in src/modules/auth/constants/default-system-roles.ts
