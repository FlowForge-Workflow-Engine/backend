/** Maximum requests per authenticated user per minute */
export const RATE_LIMIT_PER_USER_PER_MINUTE = 120;

/** Maximum requests per tenant per minute (across all users) */
export const RATE_LIMIT_PER_TENANT_PER_MINUTE = 600;

